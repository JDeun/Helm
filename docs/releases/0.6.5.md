# Helm v0.6.5 — Adoption Docs And Local Dashboard

**Date:** 2026-04-26

Helm 0.6.5 improves first-run adoption, runtime integration guidance, and local operational visibility.

## Added

- `helm status --brief` for a compact health-oriented status summary
- `helm dashboard` for a local text dashboard covering task state, checkpoints, memory review pressure, and next actions
- `helm report --format html` for a portable HTML operations report
- integration docs for Codex CLI, Claude Code, OpenClaw, OpenHands-style runtimes, and existing projects
- comparison docs clarifying how Helm differs from agent frameworks, observability tools, and eval tools
- profile-template and demo scenario docs
- sample integration scripts under `examples/integrations/`

## Changed

- README and README.ko now use a direct Quickstart instead of overlapping "5-minute first run" and install sections
- release checklist now includes brief status, dashboard, and HTML report smoke checks

## Validation

- full local pytest run after the fix: `309 passed`
- compileall passed for `scripts`, `commands`, `tests`, and top-level Python modules
- `git diff --check` passed
