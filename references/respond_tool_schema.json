{
  "type": "function",
  "function": {
    "name": "respond",
    "description": "Use this tool to deliver the final answer when no further tool calls are needed. The 'message' field becomes the user-visible response.",
    "parameters": {
      "type": "object",
      "properties": {
        "message": {
          "type": "string",
          "description": "The final response to the user."
        }
      },
      "required": ["message"]
    }
  }
}
